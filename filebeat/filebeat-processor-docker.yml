filebeat.inputs:
  - type: container
    paths:
      - "/var/lib/docker/containers/*/*.log"
    processors:
      - add_docker_metadata:

processors:
  - add_labels:
      labels:
        foo: bar

output.elasticsearch:
  hosts:
    - "localhost:9200"
